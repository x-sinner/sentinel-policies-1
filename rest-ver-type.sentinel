import "tfplan-functions" as plan
allowed_types = ["IPBASE"]
All_Types = plan.find_resources("equinix_network_device")
violatingversion = plan.filter_attribute_not_in_list(All_Types, "package_code", allowed_types, true)
violations = length(violatingversion["messages"])
main = rule {violations is 0}
